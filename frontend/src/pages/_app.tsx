// src/App.tsx

import React from "react";

const App: React.FC = () => {
  return (
    <div style={{
      display: "flex",
      height: "100vh",
      justifyContent: "center",
      alignItems: "center",
      fontSize: "3rem",
      fontWeight: "bold"
    }}>
      LumaScan Frontend is running 
    </div>
  );
};

export default App;
